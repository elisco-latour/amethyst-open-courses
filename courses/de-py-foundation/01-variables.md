---
id: "de_py_foundation_01_repo"
title: "The Blueprint repo"
type: "coding"
xp: 100
---

# The Blueprint
Welcome to the Data Engineering team. Your first task is to build a **Log Generator**. This tool will simulate user traffic for our analytics pipeline.

Before we can generate logs, we need to define the configuration for our application.
In Python, we use **variables** to store data.

## Task
Define a variable named `app_name` and assign it the value `"AmethystLogger"`.
Define a variable named `version` and assign it the value `1.0`.

<!-- SEPARATOR -->

# seed_code
# Define your configuration variables here


<!-- SEPARATOR -->

# validation_code
assert app_name == "AmethystLogger", "app_name should be 'AmethystLogger'"
assert version == 1.0, "version should be 1.0"
