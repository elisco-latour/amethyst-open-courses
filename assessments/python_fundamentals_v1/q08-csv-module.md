---
id: q_pyfun_08
assessment_id: python_fundamentals_v1
title: "Read a CSV row"
question_type: fill_blank
sub_skill_tags: [data_engineering]
xp: 10
difficulty: beginner
sort_order: 8
expected: ["csv"]
regex: false
case_sensitive: false
---

# prompt
Fill in the standard-library module name that provides `reader` and `DictReader` for parsing comma-separated files: `import ___`

<!-- SEPARATOR -->

# explanation
Python's `csv` module ships with the standard library and provides both `csv.reader` and `csv.DictReader`.
